package com.crudzaso.cityhelp.auth.domain.enums;

/**
 * User roles for the CityHelp authentication system.
 * Follows English naming convention for technical code.
 * Only two roles: USER (default) and ADMIN (administrative).
 */
public enum UserRole {
    USER,
    ADMIN
}