# CityHelp Auth Service - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# DEPLOYMENT MODE
# ============================================
# Spring profile: dev, prod
SPRING_PROFILE=prod

# ============================================
# VPS / DOMAIN CONFIGURATION
# ============================================
# IMPORTANT: When you don't have DNS yet, use your VPS IP address
# Example: VPS_IP=192.168.1.100
# When you get your DNS, replace IP with domain
# Example: VPS_IP=api.cityhelp.com (or just set APP_BASE_URL directly)
VPS_IP=YOUR_VPS_IP_HERE

# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_DB=cityhelp_auth
POSTGRES_USER=auth_service_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_PORT=5432

# ============================================
# REDIS CONFIGURATION
# ============================================
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD
REDIS_PORT=6379

# ============================================
# JWT CONFIGURATION
# ============================================
# Generate a secure 256-bit secret key
# You can use: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_256_BIT_SECRET
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# ============================================
# CORS CONFIGURATION
# ============================================
# For local development
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# For production with IP (before DNS)
# CORS_ALLOWED_ORIGINS=http://YOUR_VPS_IP:3000,http://YOUR_VPS_IP

# For production with DNS (after DNS setup)
# CORS_ALLOWED_ORIGINS=https://cityhelp.com,https://app.cityhelp.com

CORS_ALLOWED_ORIGINS=http://localhost:3000

# ============================================
# OAUTH2 GOOGLE CONFIGURATION
# ============================================
# Get these from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
OAUTH2_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
OAUTH2_GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# Redirect URI (update when you have DNS)
# Before DNS: http://YOUR_VPS_IP:8001/login/oauth2/code/google
# After DNS: https://api.cityhelp.com/login/oauth2/code/google
OAUTH2_GOOGLE_REDIRECT_URI=http://localhost:8001/login/oauth2/code/google

# ============================================
# EMAIL CONFIGURATION (SMTP)
# ============================================
# For Gmail: smtp.gmail.com, port 587
# For other providers, check their SMTP settings
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_FROM=noreply@cityhelp.com

# ============================================
# APPLICATION URLS
# ============================================
# These will be used in email templates and OAuth redirects
# Before DNS: use VPS IP
# After DNS: change to your domain

# Backend URL
# Before DNS: http://YOUR_VPS_IP:8001
# After DNS: https://api.cityhelp.com
APP_BASE_URL=http://localhost:8001

# Frontend URL (for email links, OAuth redirects)
# Before DNS: http://YOUR_VPS_IP:3000
# After DNS: https://cityhelp.com
FRONTEND_URL=http://localhost:3000

# ============================================
# SERVICE PORTS (Optional - leave default)
# ============================================
AUTH_SERVICE_PORT=8001

# ============================================
# ANDROID APP CONFIGURATION
# ============================================
# This is the base URL that Android will use to connect
# Copy this value to your Android app's configuration
# Path: mobile_cityhelp/android/app/src/main/res/values/api_config.xml
#
# Before DNS: http://YOUR_VPS_IP:8001
# After DNS: https://api.cityhelp.com
ANDROID_API_BASE_URL=http://localhost:8001
