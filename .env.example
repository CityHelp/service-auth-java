# CityHelp Auth Service - Environment Variables
# Copia este archivo a .env y completa con tus valores reales

# ===========================================
# PostgreSQL Database (Render o local)
# ===========================================
# Opción 1: URL completa (Render te da esto)
DB_URL=jdbc:postgresql://localhost:5432/cityhelp_auth

# Opción 2: Componentes separados (si necesitas más control)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cityhelp_auth
DB_USERNAME=auth_service_user
DB_PASSWORD=your_secure_password_here

# ===========================================
# Redis Configuration
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===========================================
# Google OAuth2 Configuration
# ===========================================
# Obtener en: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxx

# ===========================================
# SMTP Email Configuration
# ===========================================
# Gmail example (requiere "App Password" si usas Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=noreply@cityhelp.com
SMTP_PASSWORD=your-app-password-here

# ===========================================
# JWT Configuration
# ===========================================
# IMPORTANTE: Genera una clave segura (mínimo 256 bits)
# Ejemplo: openssl rand -base64 32
JWT_SECRET=your-256-bit-secret-key-change-in-production

# ===========================================
# Application Environment
# ===========================================
ENVIRONMENT=development
